---
name: "EncodingWarning"
description: "Raised (Python 3.10+) when the locale encoding is used implicitly, for example when open() is called without an explicit encoding argument."
references:
  - https://docs.python.org/3/library/exceptions.html
---
