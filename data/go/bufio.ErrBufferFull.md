---
name: "Buffer Full"
description: "Returned by bufio.Scanner when a single token (line, word, etc.) is larger than the scanner's buffer. Call scanner.Buffer() before scanning to increase the maximum token size."
references:
  - https://pkg.go.dev/bufio#ErrBufferFull
---
