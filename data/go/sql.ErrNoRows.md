---
name: "No Rows"
description: "Returned by QueryRow.Scan when the query returns no rows. Not returned by Query — callers must check rows.Next() themselves."
references:
  - https://pkg.go.dev/database/sql#ErrNoRows
---
