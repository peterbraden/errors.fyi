---
name: "Profiling Timer Expired (SIGPROF)"
description: "The container process was terminated by SIGPROF (signal 27) when a profiling interval timer expired. This signal is used by profiling tools (e.g. gprof) to sample execution; an unhandled SIGPROF in production code indicates a misconfigured profiler."
references:
  - https://docs.docker.com/engine/reference/run/#exit-status
---
