---
name: "Window Size Changed (SIGWINCH)"
description: "The container process exited due to SIGWINCH (signal 28). SIGWINCH is normally ignored by default and is sent when the terminal window is resized, so exit code 156 indicates the process explicitly set a terminating disposition for it."
references:
  - https://docs.docker.com/engine/reference/run/#exit-status
---
