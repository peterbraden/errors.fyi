---
name: "Urgent Socket Condition (SIGURG)"
description: "The container process was terminated by SIGURG (signal 23), which is sent when out-of-band data arrives on a socket. The default action is to ignore it, so exit code 151 indicates the process explicitly reset SIGURG to its terminating default."
references:
  - https://docs.docker.com/engine/reference/run/#exit-status
---
