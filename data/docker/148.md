---
name: "Terminal Stop (SIGTSTP)"
description: "The container process was suspended by SIGTSTP (signal 20), the terminal stop signal. Unlike SIGSTOP, SIGTSTP can be caught; if unhandled, the process is stopped and the shell reports exit code 148 on subsequent termination."
references:
  - https://docs.docker.com/engine/reference/run/#exit-status
---
