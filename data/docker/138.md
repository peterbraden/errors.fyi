---
name: "Terminated by SIGUSR1"
description: "The container received SIGUSR1 (signal 10), a user-defined signal. Some applications use it to trigger log rotation, a graceful reload, or a status dump rather than terminating."
references:
  - https://docs.docker.com/engine/reference/run/#exit-status
---
