---
name: "Terminated by SIGUSR1"
description: "The container process was terminated by SIGUSR1 (signal 10), a user-defined signal. Applications that catch SIGUSR1 use it for app-specific purposes such as toggling debug logging or triggering a graceful reload; if the process does not handle it, the default action is termination."
references:
  - https://docs.docker.com/engine/reference/run/#exit-status
---
