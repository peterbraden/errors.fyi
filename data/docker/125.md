---
name: "Docker daemon error"
description: "The docker run command itself failed. This typically means the Docker daemon could not create or start the container — for example, an invalid flag was passed to docker run or the image could not be pulled."
references:
  - https://docs.docker.com/engine/reference/run/#exit-status
---
