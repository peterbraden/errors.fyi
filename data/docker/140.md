---
name: "Terminated by SIGUSR2"
description: "The container process was terminated by SIGUSR2 (signal 12), a user-defined signal. Like SIGUSR1, its meaning is application-defined; if unhandled, the default action is termination."
references:
  - https://docs.docker.com/engine/reference/run/#exit-status
---
