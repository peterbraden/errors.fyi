---
name: "Child Process Terminated (SIGCHLD)"
description: "The container process exited due to SIGCHLD (signal 17). SIGCHLD is normally ignored by default, so receiving it as a fatal exit code indicates the process explicitly set SIGCHLD to its default (terminating) disposition."
references:
  - https://docs.docker.com/engine/reference/run/#exit-status
---
