---
name: "Terminated by SIGKILL"
description: "The container was killed with SIGKILL (signal 9). Common causes: docker stop (after the stop timeout), docker kill, the Linux OOM killer terminating the container due to memory pressure, or a Kubernetes pod eviction."
references:
  - https://docs.docker.com/engine/reference/run/#exit-status
---
