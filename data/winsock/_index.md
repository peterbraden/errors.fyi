---
title: "Windows Sockets (Winsock) Error Codes"
description: "Error codes returned by the Windows Sockets API (Winsock2), retrieved via WSAGetLastError(). Distinct from generic Win32 system error codes and from the CRT errno values, these are the codes actually surfaced by socket operations (connect, send, recv, bind) on Windows."
references:
  - https://learn.microsoft.com/en-us/windows/win32/winsock/windows-sockets-error-codes-2
---
