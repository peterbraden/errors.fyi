---
name: "Network Not Found"
description: "DNS resolution failed when contacting the registry. The machine may be offline, behind a proxy that requires configuration, or pointing at an internal registry that is unreachable. Check network connectivity and the `registry` npm config value."
references:
  - https://docs.npmjs.com/cli/v10/using-npm/config#registry
---
