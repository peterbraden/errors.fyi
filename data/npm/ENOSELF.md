---
name: "Cannot Install Self"
description: "A package is trying to install itself as its own dependency, which creates a circular dependency. Check package.json dependencies for a package with the same name as the current project."
references:
  - https://docs.npmjs.com/cli/v10/commands/npm-install
---
