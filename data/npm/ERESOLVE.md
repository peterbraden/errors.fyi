---
name: "Resolve Error"
description: "npm could not resolve the dependency tree. Two packages require incompatible versions of a shared dependency. Run `npm install --legacy-peer-deps` to fall back to the npm v6 peer-dependency strategy, or update the conflicting packages so their peer-dependency ranges overlap."
references:
  - https://docs.npmjs.com/cli/v10/configuring-npm/package-json#peerdependencies
---
