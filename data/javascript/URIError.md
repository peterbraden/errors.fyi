---
name: "URIError"
description: "Thrown when a global URI-handling function such as decodeURIComponent(), decodeURI(), encodeURI(), or encodeURIComponent() is passed a malformed argument, e.g. an incomplete percent-encoded escape sequence."
references:
  - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/URIError
---
