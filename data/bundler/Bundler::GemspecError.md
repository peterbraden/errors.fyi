---
name: "GemspecError"
description: "A `.gemspec` file is missing, contains a syntax error, or omits required fields such as `name`, `version`, or `summary`. Validate the gemspec with `gem build <name>.gemspec`."
references:
  - https://guides.rubygems.org/specification-reference/
---
