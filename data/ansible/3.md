---
name: "One or More Hosts Unreachable"
description: "At least one host was unreachable (SSH connection refused, authentication failure, or host offline). Other hosts may still have run successfully. Check connectivity and credentials for the failing hosts."
references:
  - https://docs.ansible.com/ansible/latest/cli/ansible-playbook.html
---
