---
name: "One or More Hosts Failed"
description: "At least one host returned a task failure (`failed=1`). Tasks on other hosts may have completed. Review the play recap to identify which hosts failed and which tasks caused the failure."
references:
  - https://docs.ansible.com/ansible/latest/cli/ansible-playbook.html
---
