---
name: "AssertionError"
description: "Thrown when an assert(condition) call evaluates to false. Scala's assert is always active (unlike Java's, which requires -ea); use assert(cond, msg) to include a descriptive message."
references:
  - https://www.scala-lang.org/api/current/scala/Predef$.html#assert(assertion:Boolean):Unit
---
