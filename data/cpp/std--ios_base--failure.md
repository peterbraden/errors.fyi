---
name: "std::ios_base::failure"
description: "Thrown by I/O stream operations when a stream error flag (failbit or badbit) is set and exceptions are enabled on that stream via stream.exceptions()."
references:
  - https://en.cppreference.com/w/cpp/io/ios_base/failure
---
