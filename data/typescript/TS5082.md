---
name: "Cannot Use Bundler Module Resolution Without moduleResolution"
description: "The `bundler` module resolution mode requires `module` to be set to `es2015` or later. Update the `module` and `moduleResolution` settings in `tsconfig.json` to be compatible."
references:
  - https://www.typescriptlang.org/tsconfig#moduleResolution
---
