---
name: "Cannot Redeclare Block-Scoped Variable"
description: "A `let` or `const` variable is declared more than once in the same scope. Each block-scoped variable name must be unique within its scope."
references:
  - https://www.typescriptlang.org/docs/handbook/2/everyday-types.html
---
