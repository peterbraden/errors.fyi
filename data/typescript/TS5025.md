---
name: "Cannot Find tsconfig"
description: "The TypeScript compiler cannot locate a `tsconfig.json` file at the specified or default path. Ensure the file exists in the project root or pass the correct path with `--project`."
references:
  - https://www.typescriptlang.org/tsconfig
---
