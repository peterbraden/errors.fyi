---
name: "Object Is Possibly Undefined"
description: "An operation is being performed on a value that may be `undefined`. Use an `undefined` check, optional chaining (`?.`), or a non-null assertion (`!`) to handle this case."
references:
  - https://www.typescriptlang.org/docs/handbook/2/narrowing.html
---
