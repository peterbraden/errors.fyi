---
name: "Cannot Overwrite Input File"
description: "The compiler output file would overwrite one of the input source files. Adjust the `outDir` or `outFile` options in `tsconfig.json` to direct output to a different location."
references:
  - https://www.typescriptlang.org/tsconfig#outDir
---
