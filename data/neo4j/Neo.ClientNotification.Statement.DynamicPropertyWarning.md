---
name: "DynamicPropertyWarning"
description: "The query uses a dynamic property lookup (e.g. n[key]), which prevents the planner from using indexes efficiently. Consider rewriting with a static property name if possible."
references:
  - https://neo4j.com/docs/status-codes/current/
---
