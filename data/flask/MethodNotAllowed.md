---
name: MethodNotAllowed
description: "The 405 response raised automatically when a URL rule matches but the request's HTTP method isn't in the route's declared methods list, e.g. sending POST to a view only registered for GET. The response includes an Allow header listing the valid methods."
references:
  - https://werkzeug.palletsprojects.com/en/stable/exceptions/#werkzeug.exceptions.MethodNotAllowed
---
