---
name: BadRequest
description: "The 400 response raised by abort(400) or automatically when Werkzeug fails to parse the incoming request, e.g. request.get_json() hitting malformed JSON, a form body that can't be decoded, or a routing converter rejecting a URL segment."
references:
  - https://werkzeug.palletsprojects.com/en/stable/exceptions/#werkzeug.exceptions.BadRequest
---
