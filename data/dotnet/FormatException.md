---
name: "FormatException"
description: "Thrown when the format of a string argument is invalid for a parse or conversion operation. Common with int.Parse(), DateTime.Parse(), and Guid.Parse(). Use the TryParse() variants to avoid the exception when input may be malformed."
references:
  - https://learn.microsoft.com/en-us/dotnet/api/system.formatexception
---
