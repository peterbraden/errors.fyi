---
name: "Do Not Compare Types, Use isinstance()"
description: "Types are compared with == (e.g. type(x) == int) instead of using isinstance(). isinstance() is preferred as it supports subclass checks."
references:
  - https://pycodestyle.pycqa.org/en/latest/intro.html#error-codes
---
