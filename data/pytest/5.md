---
name: "No Tests Collected"
description: "pytest ran successfully but found no tests matching the given criteria. Check that test files match the `python_files` pattern (default: `test_*.py` / `*_test.py`), that test functions are prefixed with `test_`, and that any `-k` or `-m` filters are not too restrictive."
references:
  - https://docs.pytest.org/en/stable/reference/exit-codes.html
  - https://docs.pytest.org/en/stable/reference/customization.html#confval-python_files
---
