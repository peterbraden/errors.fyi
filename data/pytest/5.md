---
name: "No Tests Collected"
description: "pytest ran successfully but found no tests matching the given paths, patterns, or markers. Check that test files follow the naming convention (test_*.py or *_test.py), that functions are prefixed with test_, and that any -k or -m filters are not too restrictive."
references:
  - https://docs.pytest.org/en/stable/reference/exit-codes.html
---
