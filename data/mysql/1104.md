---
name: "SELECT would examine too many rows"
description: "The query was rejected because it would scan more rows than the max_join_size limit."
references:
  - https://dev.mysql.com/doc/mysql-errors/8.0/en/server-error-reference.html
---
