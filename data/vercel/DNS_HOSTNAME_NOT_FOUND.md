---
name: "DNS Hostname Not Found"
description: "DNS resolution for the requested hostname returned NXDOMAIN, meaning the domain does not exist. Returned with HTTP 502; verify the domain is registered and that its DNS records are configured correctly."
references:
  - https://vercel.com/docs/errors/DNS_HOSTNAME_NOT_FOUND
---
