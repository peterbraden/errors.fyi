---
name: "Router Cannot Match"
description: "Vercel's edge router could not match the incoming request to any configured route, redirect, or rewrite pattern. Returned with HTTP 502; check vercel.json and the framework's routing configuration for conflicting or malformed rules."
references:
  - https://vercel.com/docs/errors/ROUTER_CANNOT_MATCH
---
