---
name: "Too Many Re-renders"
description: "Too many re-renders. React limits the number of renders to prevent an infinite loop. This is usually caused by an unconditional setState call inside the render function or a useEffect/useMemo with a dependency that changes on every render."
references:
  - https://react.dev/errors/301
---
