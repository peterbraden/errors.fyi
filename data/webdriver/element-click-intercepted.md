---
name: "Element Click Intercepted"
description: "The requested element would not receive the click event because another element (e.g. an overlay or a different element in front) is obscuring it. Returned with HTTP 400; scroll the target into view or dismiss whatever is covering it before retrying."
references:
  - https://www.w3.org/TR/webdriver2/#errors
---
