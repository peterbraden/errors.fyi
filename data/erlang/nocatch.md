---
name: "nocatch"
description: "A `throw` was executed outside the scope of any enclosing `catch` expression or `try...catch` block. The thrown value is wrapped as `{nocatch, Value}`."
references:
  - https://www.erlang.org/doc/reference_manual/errors.html
---
