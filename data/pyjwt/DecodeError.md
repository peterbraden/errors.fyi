---
name: "DecodeError"
description: "Raised when a token cannot be decoded because it failed validation — for example the token isn't valid base64url, isn't valid JSON once decoded, or doesn't have the header.payload.signature structure a JWT requires."
references:
  - https://pyjwt.readthedocs.io/en/stable/api.html
  - https://github.com/jpadilla/pyjwt/blob/master/jwt/exceptions.py
---
