---
name: "Invalid repr Attribute Target"
description: "A #[repr(..)] attribute was placed on an item type that does not support it. For example, repr(C) is only valid on structs and enums, while repr(u8) is only valid on enums."
references:
  - https://doc.rust-lang.org/error_codes/E0517.html
---
