---
name: "name is already used by an existing object"
description: "A CREATE statement tried to create a database object (table, index, sequence, view, etc.) with a name that already exists in the schema. Either drop or rename the existing object first, or choose a different name for the new object."
references:
  - https://docs.oracle.com/en/database/oracle/oracle-database/19/errmg/ORA-00955.html
---
