---
name: "Resource already exists"
description: "Terraform attempted to create a resource but the provider reports it already exists in the target environment. Import the existing resource into state with terraform import rather than recreating it."
references:
  - https://developer.hashicorp.com/terraform/cli/import
---
