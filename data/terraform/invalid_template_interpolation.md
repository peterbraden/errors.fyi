---
name: "Invalid template interpolation value"
description: "A template string interpolation (${ }) contains a value that cannot be converted to a string, such as a list or object. Use jsonencode() or a string conversion function to produce a string representation."
references:
  - https://developer.hashicorp.com/terraform/language/expressions/strings#interpolation
---
