---
name: "DNS Points to Prohibited IP"
description: "Cloudflare resolved the hostname but the resulting IP address is one it cannot proxy to, such as a Cloudflare IP or a loopback address. Update the DNS record to point to the correct origin IP."
---
