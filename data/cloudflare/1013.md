---
name: "HTTP Hostname and TLS SNI Hostname Mismatch"
description: "The hostname in the HTTP Host header does not match the hostname sent in the TLS SNI extension. This can indicate a misconfigured client or a proxy that is not forwarding SNI correctly."
---
