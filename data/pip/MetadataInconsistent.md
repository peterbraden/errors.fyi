---
name: "MetadataInconsistent"
description: "Raised when the metadata in a built wheel is inconsistent with what was requested — for example the name or version in METADATA does not match the wheel filename. Often indicates a broken or misconfigured build backend."
references:
  - https://github.com/pypa/pip/blob/main/src/pip/_internal/exceptions.py
---
