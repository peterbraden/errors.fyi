---
name: NoUniqueBeanDefinitionException
description: "Thrown when more than one bean matches an @Autowired injection point and no single candidate is marked @Primary. Resolve by adding @Qualifier to name the intended bean, or annotate one implementation with @Primary."
references:
  - https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/beans/factory/NoUniqueBeanDefinitionException.html
---
