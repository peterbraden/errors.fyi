---
name: getStaticPaths Invalid Route Param
description: "A `params` value returned from `getStaticPaths()` has an invalid type. Route parameters must be strings (or `undefined` for optional params) — numbers, objects, and other types aren't allowed."
references:
  - https://docs.astro.build/en/reference/errors/get-static-paths-invalid-route-param/
---
