---
name: Missing Media Query Directive
description: "The `client:media` hydration directive was used without providing a media query string, e.g. `client:media`. It must be given a value such as `client:media=\"(max-width: 50em)\"` so Astro knows when to hydrate the component."
references:
  - https://docs.astro.build/en/reference/errors/missing-media-query-directive/
---
