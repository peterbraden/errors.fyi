---
name: getStaticPaths Expected Params
description: "A route object returned from `getStaticPaths()` is missing the required `params` property, so Astro doesn't know which URL the generated page should map to."
references:
  - https://docs.astro.build/en/reference/errors/get-static-paths-expected-params/
---
