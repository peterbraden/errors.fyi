---
name: getStaticPaths Required
description: "A dynamic route (e.g. `[slug].astro`) is missing the required `getStaticPaths()` export. In static (non-SSR) output mode, every dynamic route must export this function so Astro knows which pages to generate at build time."
references:
  - https://docs.astro.build/en/reference/errors/get-static-paths-required/
---
