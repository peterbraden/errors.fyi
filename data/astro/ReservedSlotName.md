---
name: Reserved Slot Name
description: "A `<slot>` was given a name that conflicts with a name reserved internally by Astro (such as `default`'s framework-specific equivalents), so the slot can't be created as written."
references:
  - https://docs.astro.build/en/reference/errors/reserved-slot-name/
---
