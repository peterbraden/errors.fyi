---
name: Image Missing Alt
description: "An `<Image />` or `<Picture />` component is missing the required `alt` property. Alt text is required for accessibility — pass a descriptive string, or `alt=\"\"` for purely decorative images."
references:
  - https://docs.astro.build/en/reference/errors/image-missing-alt/
---
