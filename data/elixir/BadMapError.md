---
name: "BadMapError"
description: "Raised when a map is expected, e.g. when using the map[key] access syntax or the %{map | key: value} update syntax on a non-map value."
references:
  - https://hexdocs.pm/elixir/BadMapError.html
---
