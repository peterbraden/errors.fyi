---
name: "Protocol Error"
description: "Raised when a protocol function is called on a value whose type has no implementation of that protocol. Implement the protocol for the type, or use `Any` as a fallback."
references:
  - https://hexdocs.pm/elixir/Protocol.html
---
