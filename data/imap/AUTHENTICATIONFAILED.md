---
name: "AUTHENTICATIONFAILED"
description: "Authentication failed because the credentials were rejected; the user exists but the password or token was wrong. Distinct from UNAVAILABLE, which indicates a transient server-side problem."
references:
  - https://www.rfc-editor.org/rfc/rfc5530#section-3
---
