---
name: "Memory limit exceeded"
numeric: 241
description: "Query or server memory usage exceeded the configured limit (`max_memory_usage` or `max_server_memory_usage`). Optimise the query, reduce result set size, or increase the limit."
references:
  - https://clickhouse.com/docs/en/operations/error-codes
---
