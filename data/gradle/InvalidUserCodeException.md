---
name: "Invalid User Code Exception"
description: "The build script called a Gradle API in an incorrect way or at an incorrect lifecycle phase — for example, mutating task inputs after the configuration phase has ended. Check the stack trace for which API was called and move it to the correct phase."
references:
  - https://docs.gradle.org/current/javadoc/org/gradle/api/InvalidUserCodeException.html
---
