---
name: "Unknown Project Exception"
description: "A Gradle build script referenced a subproject that does not exist or has not been included in settings.gradle. Verify the project path (e.g. ':subproject-name') and that the project is declared with include() in settings.gradle."
references:
  - https://docs.gradle.org/current/javadoc/org/gradle/api/UnknownProjectException.html
---
