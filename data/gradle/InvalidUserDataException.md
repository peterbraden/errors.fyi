---
name: "Invalid User Data Exception"
description: "The build script or plugin passed an invalid value to a Gradle API — for example a null where a non-null is required, or a value that fails validation. Review the task or extension configuration indicated in the stack trace."
references:
  - https://docs.gradle.org/current/javadoc/org/gradle/api/InvalidUserDataException.html
---
