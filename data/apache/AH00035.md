---
name: "access to path denied"
description: "A request was denied because the filesystem path falls outside the document root or a configured alias, or because no Options Indexes is set for a directory with no DirectoryIndex file. Results in a 403 Forbidden response."
references:
  - https://httpd.apache.org/docs/current/mod/core.html#options
---
